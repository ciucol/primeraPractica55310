const fs = require('fs')

class UsersFileDao {
  async findAll() {
    return await 
  }

  async insertOne(newUserInfo) {
    const newUser = 
    return newUser._id
  }
}

module.exports = UsersFileDao
